//let baseURL = 'rbttest.fingard.cn'
//let webURL = `http://${baseURL}/manage`
let webURL = "/manage";
// let wsURL = `ws://${baseURL}/manage`
// let reconURL = `http://${baseURL}/recon`

export { webURL };
